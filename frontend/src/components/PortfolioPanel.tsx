import { useEffect, useState } from 'react';
import { api, PortfolioData, Trade } from '../lib/api';
import { compactVolume, pct, won } from '../lib/format';
export default function PortfolioPanel({ onSelect }: { onSelect: (ticker: string) => void }) {
  const [data, setData] = useState<PortfolioData | null>(null); const [trades, setTrades] = useState<Trade[]>([]); const [cash, setCash] = useState(''); const [form, setForm] = useState({ ticker: '', side: 'buy', trade_date: new Date().toISOString().slice(0,10), quantity: '', price: '', fee: '0', tax: '', memo: '' }); const [message, setMessage] = useState('');
  const refresh = () => { api.portfolio().then(setData).catch(() => undefined); api.trades().then(setTrades).catch(() => undefined); };
  useEffect(refresh, []);
  const submitTrade = async (event: React.FormEvent) => { event.preventDefault(); try { await api.addTrade({ ticker: form.ticker, side: form.side as 'buy' | 'sell', trade_date: form.trade_date, quantity: Number(form.quantity), price: Number(form.price), fee: Number(form.fee), tax: Number(form.tax || 0), memo: form.memo || null }); setMessage('거래가 추가되었습니다.'); refresh(); } catch (error) { setMessage(error instanceof Error ? error.message : '거래 추가 실패'); } };
  const updateCash = async () => { try { await api.cash(Number(cash)); setMessage('현금을 저장했습니다.'); refresh(); } catch (error) { setMessage(error instanceof Error ? error.message : '현금 저장 실패'); } };
  if (!data) return <div className="panel empty">포트폴리오를 불러오는 중…</div>;
  const stats = [['총 평가금액',won(data.total_market_value)],['총 매입금액',won(data.total_cost)],['평가손익',won(data.total_unrealized)],['수익률',pct(data.total_unrealized_pct)],['당일 손익',won(data.total_day_change)],['실현손익',won(data.total_realized)],['현금',won(data.cash_krw)],['총자산',won(data.total_assets)]];
  return <div className="portfolio-layout"><div className="card-grid">{stats.map(([label,value]) => <div className="stat-card" key={label}><label>{label}</label><strong className={label.includes('손익') ? (String(value).startsWith('-') ? 'blue' : 'red') : ''}>{value}</strong></div>)}</div><div className="panel table-panel"><div className="toolbar"><div className="section-title" style={{ margin: 0 }}>POSITIONS</div>{data.stale && <span className="badge warning">일부 시세 오래됨</span>}</div><table className="metric-table"><thead><tr>{['종목','수량','평균단가','현재가','평가금액','평가손익','수익률','당일손익','비중'].map(label => <td key={label}><b>{label}</b></td>)}</tr></thead><tbody>{data.positions.map(position => <tr key={position.ticker} onClick={() => onSelect(position.ticker)} style={{ cursor: 'pointer' }}><td><b>{position.name}</b><span className="muted mono"> {position.ticker}</span></td><td className="mono">{position.quantity}</td><td className="mono">{won(position.avg_cost)}</td><td className="mono">{won(position.last_close)}</td><td className="mono">{won(position.market_value)}</td><td className={position.unrealized >= 0 ? 'red mono' : 'blue mono'}>{won(position.unrealized)}</td><td className="mono">{pct(position.unrealized_pct)}</td><td className="mono">{won(position.day_change)}</td><td className="mono">{pct(position.weight)}</td></tr>)}</tbody></table>{!data.positions.length && <div className="empty" style={{ height: 120 }}>아직 보유 종목이 없습니다.</div>}</div><div className="panel table-panel"><div className="section-title">TRADE LEDGER</div><form className="trade-form" onSubmit={submitTrade}>{[['ticker','티커'],['trade_date','거래일'],['quantity','수량'],['price','가격'],['fee','수수료'],['tax','세금'],['memo','메모']].map(([key,placeholder]) => <input key={key} required={['ticker','quantity','price'].includes(key)} placeholder={placeholder} type={key === 'trade_date' ? 'date' : key === 'quantity' || key === 'price' || key === 'fee' || key === 'tax' ? 'number' : 'text'} value={form[key as keyof typeof form]} onChange={e => setForm(current => ({ ...current, [key]: e.target.value }))} />)}<select value={form.side} onChange={e => setForm(current => ({ ...current, side: e.target.value }))}><option value="buy">매수</option><option value="sell">매도</option></select><button className="primary">거래 추가</button></form><div className="trade-form" style={{ marginTop: 9 }}><input type="number" placeholder={`현금 ${compactVolume(data.cash_krw)}`} value={cash} onChange={e => setCash(e.target.value)} /><button className="ghost" onClick={updateCash}>현금 저장</button><span className="subtle">{message}</span></div><table className="metric-table" style={{ marginTop: 14 }}><tbody>{trades.map(trade => <tr key={trade.id}><td>{trade.trade_date}</td><td className={trade.side === 'buy' ? 'red' : 'blue'}>{trade.side === 'buy' ? '매수' : '매도'}</td><td>{trade.name || trade.ticker}</td><td className="mono">{trade.quantity}주 × {won(trade.price)}</td><td><button className="danger" onClick={() => api.deleteTrade(trade.id).then(refresh).catch(error => setMessage(error.message))}>삭제</button></td></tr>)}</tbody></table></div></div>;
}
